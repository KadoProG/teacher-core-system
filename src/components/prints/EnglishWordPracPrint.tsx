import { Box } from '@mui/material';

/**
 * # 単語テスト
 */
export const EnglishWordPracPrint = () => (
  <Box px={10} position="relative" height={1080}>
    <Box py={2} height={80} position="absolute" bottom={0}>
      ここがﾌｯﾀ
    </Box>
  </Box>
);
